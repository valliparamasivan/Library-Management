"use client";
import { SidebarMenu } from "@/components/ui/sidebar";

const MainProfile = ({ user }) => {
  return (
    <SidebarMenu className="px-3 group-data-[collapsible=icon]:px-0">
      {/* Profile and Logout are now in header */}
    </SidebarMenu>
  );
};
export default MainProfile;
